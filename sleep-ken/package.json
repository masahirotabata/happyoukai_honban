{
  "name": "sleep-ken",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/native.ts --bundle --outfile=www/native.js --format=iife --platform=browser && mkdir -p www && cp -R public/* www/",
    "sync:ios": "npx cap sync ios",
    "appflow": "npm run build && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor-community/admob": "^5.0.0",
    "@capacitor/ios": "^5.7.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.8",
    "@capacitor/core": "^5.7.8",
    "esbuild": "^0.21.0"
  }
}
